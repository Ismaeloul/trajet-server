# Copia este fichero como .env (NUNCA se sube a git) y rellena lo que haga falta.
# Clave de PRIM: opcional desde la 0.4.0. Si la guardas en el panel, manda la
# del panel y esta queda solo como alternativa.
PRIM_API_KEY=
# Direcciones que van en el QR de emparejamiento (se pueden cambiar en el panel).
TRAJET_LAN_URL=http://192.168.1.188:7796
TRAJET_TAILSCALE_URL=http://100.99.38.76:7796
