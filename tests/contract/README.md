# Contrato

- `openapi.yaml`: contrato de la v2 (0.4.0). **La fuente es `trajet-ios/docs/openapi.yaml`**; esta es una copia para que los tests del servidor no dependan del otro repo. Si cambia, se copia con `scripts/sync-contract.sh`.
- `openapi-0.3.0.yaml`: la API de la 0.3.0, derivada del codigo de produccion.
- `openapi-live-0.3.0.json`: el `openapi.json` que servia produccion el 24-09-2026.
